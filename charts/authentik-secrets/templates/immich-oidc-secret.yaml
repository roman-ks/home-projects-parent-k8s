apiVersion: v1
kind: Secret
metadata:
  name: immich-oidc-secret
  namespace: authentik
data:
  client-id: {{ .Values.immich.oidc.clientId | b64enc }}
  client-secret: {{ .Values.immich.oidc.clientSecret | b64enc }}
