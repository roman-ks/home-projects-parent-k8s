apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{ .Values.localPathProvisioner.storageClassName }}
provisioner: {{ .Values.localPathProvisioner.provisionerName }}
reclaimPolicy: {{ .Values.localPathProvisioner.reclaimPolicy }}
volumeBindingMode: WaitForFirstConsumer
