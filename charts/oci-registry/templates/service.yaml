apiVersion: v1
kind: Service
metadata:
  name: oci-registry
spec:
  selector:
    app.kubernetes.io/name: oci-registry
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      name: http