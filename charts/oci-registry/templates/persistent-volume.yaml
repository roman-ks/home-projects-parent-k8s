apiVersion: v1
kind: PersistentVolume
metadata:
  name: oci-registry-pv
spec:
  capacity:
    storage: 15Gi
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: {{ .Values.oci_registry.pvHostPath }}