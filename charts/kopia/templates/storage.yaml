apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "kopia.fullname" . }}-source
  labels:
    {{- include "kopia.labels" . | nindent 4 }}
  annotations:
    # Shared data (immich/samba) — never touch it on uninstall.
    helm.sh/resource-policy: keep
spec:
  capacity:
    storage: {{ .Values.source.size }}
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  local:
    path: {{ .Values.source.hostPath }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - {{ .Values.nodeName }}
  claimRef:
    namespace: {{ .Release.Namespace }}
    name: {{ include "kopia.fullname" . }}-source
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "kopia.fullname" . }}-source
  labels:
    {{- include "kopia.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: {{ include "kopia.fullname" . }}-source
  resources:
    requests:
      storage: {{ .Values.source.size }}
