apiVersion: v1
kind: Service
metadata:
  name: pihole
spec:
  selector:
    app.kubernetes.io/name: pihole
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http