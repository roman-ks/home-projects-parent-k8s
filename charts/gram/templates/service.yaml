apiVersion: v1
kind: Service
metadata:
  name: gram
spec:
  selector:
    app.kubernetes.io/name: gram
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http