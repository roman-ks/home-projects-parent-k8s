name: learn-github-actions
run-name: ${{ github.actor }} is checking ARC runner
on: [push]
jobs:
  print-runner-info:
    runs-on: arc-runner-set
    steps:
      - name: Check runner
        run: |
            echo "This is ARC runner $RUNNER_NAME"
            uname -a
            uname -m
